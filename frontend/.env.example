# Base URL of the KilimoLens FastAPI backend (graph-AI credit scoring).
# Local dev default is http://localhost:8000/api. Set this to your deployed
# backend URL in production (e.g. https://api.kilimolens.app/api).
VITE_API_URL=http://localhost:8000/api
