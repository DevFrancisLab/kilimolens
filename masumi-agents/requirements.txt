fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.1
python-dotenv==1.0.1
httpx==0.28.1
neo4j==5.27.0
# Masumi SDK (optional at runtime — the payment layer falls back to a local mock
# when it or the credentials are absent, so the agents run offline for demos).
masumi==0.1.5
